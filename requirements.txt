# Build Tools (CRITICAL: Must be at the top to build dlib)
cmake

# The Missing Libraries (Fixes your warning)
dlib
face_recognition

# Django
Django>=5.1
djangorestframework
gunicorn
whitenoise

# Image Processing
opencv-python-headless
Pillow
numpy

# DeepFace (You can keep this if your code uses it too)
deepface
tf-keras
tensorflow

# HTTP Requests
requests